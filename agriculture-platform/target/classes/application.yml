server:
  port: 8080
  servlet:
    context-path: /

spring:
  datasource:
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver
    url: jdbc:sqlserver://localhost:1433;databaseName=agriculture_db;trustServerCertificate=true;encrypt=false
    username: agriculture_user
    password: Agriculture123!

  thymeleaf:
    cache: false
    encoding: UTF-8
    mode: HTML
    prefix: classpath:/templates/
    suffix: .html

mybatis:
  configuration:
    map-underscore-to-camel-case: true